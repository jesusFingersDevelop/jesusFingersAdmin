import React from 'react';

type Props = {
  date: string;
};

const DayCalendar = ({date}: Props) => {
  console.log(date);
  return (
    <>
      <div></div>
    </>
  );
};

export default DayCalendar;
